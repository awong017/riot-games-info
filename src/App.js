import React from "react";
import Styled from "styled-components";

const AppDiv = Styled.div`
  text-align: center;
`

const App = () => {
  return (
    <AppDiv>
      This is my app
    </AppDiv>
  );
};

export default App;
